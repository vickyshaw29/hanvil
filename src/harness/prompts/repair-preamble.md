You are repairing an in-place extension of an existing application.
Preserve unrelated working features. Prefer the smallest fix that clears the findings.
{{#chainReset}}
The local chain was reset to the state before your previous attempt; contracts, topics and accounts it created no longer exist. Redeploy or recreate them — `chainValidation.deploy.commands` run again every attempt.
{{/chainReset}}
